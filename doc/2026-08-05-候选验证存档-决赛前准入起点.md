# 2026-08-05 候选验证存档 — 决赛前准入起点

日期：2026-08-05
证据范围：`local_ascend_910b4`（本机 910B4，非官方 A2/A3）
用途：本文冻结当日在本机验出的候选结果，作为**决赛前**干净走完整准入流水线的现成起点。不改任何已提交产物，不覆盖当前官方成绩。

## 0. 当前官方成绩（已 banked，勿动）

- 得分 **32.00**，AC(54/63)，19/21 kernels，avg_speedup=32.25，提交 21/21。
- 提交时间 2026-08-05 18:16:25。
- 对应 ZIP：`output/local-qualification/t4-real-agent-batch-smoke.zip`
- ZIP SHA-256：`a895a79e4f76b418f86c078d81f80dd2cb2d4b38f0f922c6fe087204ab769904`
- 详细逐算子得分见 `doc/2026-08-05-官方评测结果快照-得分32.md`。

这是目前史上最好成绩。除非有经官方验证更好的包，否则不重交。

## 1. 本日新验证结果（本机 correctness，单可见 case）

针对两个官方失败算子，验证了保守备选的本机 correctness。证据 JSON 在 `output/multislot-val/`，原始运行日志 `output/multislot-val/run.log`。

| 算子 | 候选 | 改动类型 | 本机 correctness | 备注 |
| --- | --- | --- | --- | --- |
| `_count_expert_num_tokens` | `localv-b98b1ae9225c` | block_size_cap | ✅ 通过（已有完整准入 + holdout） | 早前已准入，标 `v3_functional_fallback_only`，本机 worst_case_ratio 1.077 |
| `_count_expert_num_tokens` | `localv-b87839e08158` | block_size_cap | ✅ 通过（仅 raw correctness） | 本日验证，缺 holdout/paired/准入块 |
| `_count_expert_num_tokens` | `localv-7755944cd0fd` | adaptive_small_input_block_cap | ✅ 通过（仅 raw correctness） | 本日验证，缺 holdout/paired/准入块 |
| `_set_k_and_s_triton_kernel` | `localv-15170c05aa4e` | page_stride_fp16_control | ✅ 通过（仅 raw correctness） | 本日验证，缺 holdout/paired/准入块 |
| `_set_k_and_s_triton_kernel` | `localv-40e6c671b344` | restore_fp8_page_layout_contract | ❌ 失败 | 本机 correctness 挂，已排除 |

## 2. 官方失败现状（32 分包里的 v1）

| 算子 | 提交的 v1 | v1 改动 | 官方结果 |
| --- | --- | --- | --- |
| `_count_expert_num_tokens` | `localv-88d977ca627b` | triton_range_loop_lowering（控制流重写） | tc2 运行时崩溃（2/3） |
| `_set_k_and_s_triton_kernel` | `localv-0f9f93e81efd` | restore_fp8_page_layout_raw_bytes（candidate_only_functional 准入） | tc1/tc2/tc3 精度全挂（0/3） |

诊断：count_expert 的 v1 是控制流重写、崩在 tc2；本日验过的两个保守备选（block_size_cap 系）撞该崩溃点的先验更低。set_k 的 v1 走 candidate_only_functional 准入（本机看着修好、官方精度全挂），本轮证实该准入路子不可靠。

## 3. 决赛前要补的工序（本日未做）

把上述本日验过的备选真正填进包，需要完整准入流水线，本日只完成了第一步（raw correctness）：

1. holdout case 生成（独立于搜索/prompt）。
2. paired benchmark（baseline vs candidate，同 device/频率）。
3. 组装带 artifact-SHA 的准入块（correctness_evaluation + holdout_evaluation + decision）写回候选 manifest。
4. 多槽位重打包（`build_official_agent_batch_smoke.py`，selection manifest 用 `candidates` 数组）。
5. 用 armed 后的 shape 门复验全部候选（见 §5）。

参考已准入样本：`output/real-agent-candidates/_count_expert_num_tokens/localv-b98b1ae9225c.manifest.json` 及其证据目录 `output/overfit-probes/count-expert-case2/`。

## 4. 收益与风险的诚实评估

- count_expert 现为 2/3。即便备选官方也过、翻成 3/3，只 +1/63 通过率；且备选本机 ratio > 1.0（比 baseline 慢），性能分 ≈ 0。对性能主导的总分几乎不动。
- set_k 现为 0/3。若 `15170c05` 官方隐藏 case 也过则翻身，但其官方历史三个全挂、先验差。
- 通用盲区：本机单 case correctness ≠ 官方 tc1/tc2/tc3 全过。本机通过不能外推为官方通过。
- 多槽位 max() 规则下，补合法准入的备选是非负操作（只取最高、不拉低），但前提是准入块 SHA 全部正确，否则产坏包。

## 5. 本日代码改动（已并入工作树，442 测试通过）

- `work/official_triton_agent/contract_executor.py`：
  - 新增 `visible_shape_values_from_test()`，从测试 AST 抽取张量维度；`interface_contract_error` 在有 test_code、未显式传 shape 值时自动武装 shape 指纹门（此前生产路径恒空转）。
  - 删除零调用、docstring 误导的 `deterministic_candidate_gate_error`（四门实际由 `interface_contract_error` 覆盖）。
- `tests/test_official_contract_executor.py`：原测该死函数的用例重指到 live 的 `interface_contract_error`，保留 `holdout_gate_error` 的 value-contamination 覆盖。

注意：shape 门武装是在 32 分 ZIP 打包之后。下次重新打包会用 armed 门复验全部 21 候选；若某候选有 baseline 没有的 `== <观测shape>` 比较会被拒（真拒=硬编码，误拒=换候选）。当前 32 分 ZIP 字节未动，不受影响。

## 6. 人工验收入口

- 官方 32 分详情：`doc/2026-08-05-官方评测结果快照-得分32.md`
- 本日 correctness 证据：`output/multislot-val/*.correctness.json` 与 `output/multislot-val/run.log`
- 已准入样本参考：`output/real-agent-candidates/_count_expert_num_tokens/localv-b98b1ae9225c.manifest.json`
